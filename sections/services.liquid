<!DOCTYPE html>
<html>
<head>
   <meta charset="UTF-8"> <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <style>
        .services {
            padding: 50px 0;
            text-align: center;
        }

        .services h2 {
            font-size: 36px;
            font-weight: bold;
            color: #333;
            margin-bottom: 20px;
        }

        .services ul {
            list-style: none;
            padding: 0;
            margin: 0;
            text-align: left;
            max-width: 800px;
            margin: 0 auto;
        }

        .services ul li {
            margin-bottom: 20px;
        }

        .services ul li p {
            font-size: 18px;
            line-height: 1.5;
        }

        /* Efecto de animación al aparecer en la pantalla */
        .fade-in {
            opacity: 0;
            transform: translateY(50px);
            transition: opacity 0.5s ease, transform 0.5s ease;
        }

        .fade-in.in-view {
            opacity: 1;
            transform: translateY(0);
        }

        /* Estilos del botón */
        .btn {
            display: inline-block;
            padding: 10px 20px;
            background-color: #333;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            cursor: pointer;
        }

        .btn:hover {
            background-color: #8ca4bc;
        }
    </style>
</head>
<body>

<div class="services">
    <div class="container">
        <h2>{{ "Why Choose Us?" }}</h2>
        <ul>
            <li class="fade-in"><p><strong>ROI Potential:</strong> Whether you're looking to buy a fixer-upper or renovate your current home for sale, we'll guide you through potential returns for your projects.</p></li>
            <li class="fade-in"><p><strong>Design:</strong> Our interior design expertise is the perfect guide through your design options and coordinating contractors to complete home improvements.</p></li>
            <li class="fade-in"><p><strong>Marketing:</strong> Staging consultation, professional photography, and a sophisticated digital marketing plan accompany every listing to reach today's buyers.</p></li>
            <li class="fade-in"><p><strong>Trajectory:</strong> Fifteen years of real estate experience, excellent customer service, and a commitment to hard work, listening, and moving forward. We provide quality service to build relationships with clients and, most importantly, maintain those relationships by communicating effectively.</p></li>
        </ul>
        <button onclick="window.location.href='https://tienda-real-trust.myshopify.com/pages/about-us'" class="btn">Learn More</button>
    </div>
</div>

<script>
    // Agregar clase de animación cuando el elemento está en la vista
    document.addEventListener('DOMContentLoaded', function () {
        const services = document.querySelector('.services');
        const servicesPosition = services.getBoundingClientRect().top;
        const screenHeight = window.innerHeight;

        function fadeInElements() {
            const elements = document.querySelectorAll('.fade-in');
            elements.forEach(function (element) {
                if (element.getBoundingClientRect().top < screenHeight) {
                    element.classList.add('in-view');
                }
            });
        }

        window.addEventListener('scroll', function () {
            if (servicesPosition < screenHeight) {
                fadeInElements();
            }
        });
    });
</script>

</body>
</html>

